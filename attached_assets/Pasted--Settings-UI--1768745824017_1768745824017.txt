你是资深前端工程师，请在我的项目里实现一个“设置(Settings)”页面，UI和交互尽量复刻我提供的截图（探探设置页风格），要求移动端优先、布局不溢出、长列表可滚动、开关/滑条样式接近原图。

技术栈要求：
- 如果项目是 React Native / Expo：用 React Native 组件实现（ScrollView、Switch、Slider 等）
- 如果项目是 React Web：用 React + CSS 实现（input[type=range]、checkbox switch、flex 布局）
你需要先读取项目当前使用的技术栈与路由方式，按现有项目来写，不要让我改一堆结构。

页面结构（按截图从上到下）：
1) 顶部导航栏
- 左侧返回按钮（<）
- 标题：设置
- 右侧不需要按钮（或按已有导航规范）

2) “发现探探新版本”一行
- 左文案：发现探探新版本
- 右侧绿色按钮/文字：更新
- 行高约 56，左右 padding 16，底部分割线

3) “向我显示”分组标题（橙色）
- 标题样式：橙色、小号字、加粗

4) 位置(Location)行
- 左：位置
- 右：任意更改定位 >（橙色文字），下方小灰字显示城市（例如：广州）
- 下方提示一行橙色小字：未开启定位权限，当前定位可能存在偏差

5) 搜索距离（Slider）
- 左：搜索距离
- 右：当前值 15km（橙色）
- 下方一条横向滑条（range/Slider）
- 滑条可拖动，实时更新右侧 km 数值（例如 1km-100km）

6) 自动扩大搜索距离（Switch）
- 左：自动扩大搜索距离
- 下方灰色说明：当附近用户较少时，自动适量扩大搜索距离
- 右侧开关 Switch（默认开启）

7) 不让距离太近的人看到我（Slider）
- 左：不让距离太近的人看到我
- 下方一条滑条（例如 0km-10km），默认在靠左位置
- 也可在右侧显示当前 km（可选，但建议有）

8) 显示性别
- 左：显示性别
- 右：女生（橙色，可点击进入选择页或弹出选择：女生/男生/全部）
- 右侧箭头 >

9) 年龄（Range Slider 双端）
- 左：年龄
- 右：18-28（橙色）
- 下方双端范围滑条（最小 18 最大 60），支持拖动两端，实时更新显示 18-28

10) 优先推荐真人实名认证用户（Switch）
- 左：优先推荐真人实名认证用户
- 右侧 Switch（默认关闭）

11) 隐私保护 SVIP 分组（带 SVIP 标签）
- 分组标题：隐私保护（橙色）
- 右侧小胶囊标签：SVIP（黄色/金色底，白字）

12) 隐私开关列表（全部默认关闭）
- 只对我右滑过的人可见（说明：开启后只有你右滑过的人才能滑到你）
- 隐藏我的距离和位置（说明：开启后别人将无法看到与你的距离和你的位置信息）
- 隐藏我的年龄
- 冻结最后活跃时间（说明：将你的活跃时间冻结在此刻不再更新）
- 隐藏会员身份（说明：开启后将隐藏 SVIP 标识及红色昵称）
每项右侧一个 Switch，项与项之间有分割线，说明文字灰色小号。

13) 应用设置分组
- 列表项：个人信息（副标题：编辑姓名等）>
- 隐私与权限（副标题：联系的人，我的相册，动态）> 右侧有橙色小圆点提示
- 消息提醒与聊天（副标题：通知，聊天设置）>
- 直播设置（副标题：直播间相关功能设置）>
- 数据和缓存（副标题：数据和缓存设置）>
- 小组件设置（副标题：设置桌面组件，获取最新内容）> 右侧橙色小圆点提示
- 账号与安全（副标题：修改手机号和密码）>
- 系统设置（副标题：系统权限管理）>
- 新手引导（副标题：常见问题汇总）>
- 分享（副标题：分享给好友）>
- 版本更新（副标题：当前版本：7.0.1）>

14) 底部按钮
- 一个居中大按钮：切换账号（右侧橙色小点）
- 按钮宽度接近屏幕，圆角 8-12，浅灰背景

样式要求：
- 主题色：橙色 #FF7A00 左右（接近即可）
- 背景：白色
- 分割线：#EEE
- 主标题字 16-18，副标题 12-13 灰色
- 所有行的右侧内容不许溢出屏幕，必须使用 flex 布局、文本省略
- 整页必须 ScrollView/overflow scroll，适配小屏

交互/状态管理：
- 使用一个 settings state 对象存储所有开关与滑条数值
- Slider/Range 改动要实时更新显示
- 点击带箭头的条目，先用 placeholder：console.log 或 navigate 到空页面（如果项目已有路由就用）
- 所有默认值：
  - 搜索距离：15km
  - 自动扩大搜索距离：开
  - 不让距离太近的人看到我：0km 或 1km
  - 显示性别：女生
  - 年龄：18-28
  - 其余开关：关

输出要求（非常重要）：
- 直接给我“可运行的完整代码”，不要只给片段
- 如果要新增文件，请告诉我新增哪些文件、完整路径、每个文件完整内容
- 如果要修改现有文件，请给出修改后的完整文件内容
- 先实现 UI 和本地 state，不要接后端
- 最后给我一个简短说明：怎么打开这个 Settings 页面（路由/入口在哪里）